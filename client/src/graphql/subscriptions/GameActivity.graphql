subscription GameActivity($gameId: String!) {
	gameActivity(gameId: $gameId) {
		id
		description
		data
		type
		game {
			id
			code
			playerCount
			status
			teams
			players {
				id
				team
				hand
				user {
					id
					name
					avatar
				}
			}
		}
	}
}
