import { StyleSheet } from "react-native";

export default StyleSheet.create({
	wrapper: {
		flex: 1,
		justifyContent: "center",
		alignItems: "center"
	},

	card: {
		padding: "10vh",
		textAlign: "center"
	},

	logo: {
		width: 250,
		height: 250
	}
});
