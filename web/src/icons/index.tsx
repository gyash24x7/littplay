import { Icon } from "@ui-kitten/components";
import React from "react";

export const GoogleIcon = (style: any) => <Icon name="google" {...style} />;

export const LogOutIcon = (style: any) => <Icon name="log-out" {...style} />;
